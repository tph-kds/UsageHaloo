# REAL_USAGE_V2_BASELINE — Task 0 reconnaissance (2026-09-14, rev 9e5be2f)

Baseline: branch `main`, rev `9e5be2f`, `python scripts/validate.py` = PASS. Dirty state preserved, not reset.

1. Actual frontend path: `apps/desktop-ui/src/` (App.svelte root, lib/api.ts live routing, lib/mock.ts demo-gated, lib/types.ts|lifecycle.ts|contracts.ts|catalog.ts|range.ts, components/ ProviderCard EdgeRail ProviderPopover DetailModal TrendChart Heatmap RangePicker AddProviderWizard WelcomeEmpty UsageRing Logo). Tauri shell `apps/desktop-ui/src-tauri/src/` (lib.rs commands, main.rs, legacy_adapter.rs). Static demo `demo/` (index.html, app.js real-data-only, widget.html).
2. Actual backend/local-server path: `prototype/server.mjs` (:4897 API + static), `collectors/*.mjs` (local, store, scheduler, daemon, pollers, reconcile, forecast, alerts, providers, codex-app-server, gemini-otlp, snapshot), Rust `crates/usage-halo-{core,collector,projection,reconcile,storage,scheduler,secrets,forecast}` + `connectors/{claude-code,codex,gemini-cli,openai,anthropic,openrouter}`.
3. Current provider definitions: `packages/brand-registry/providers.json` (24 ids, p0/p1/p2/p3); icons `assets/providers/icon-manifest.json`; connectors only for 6 ids (claude-code, codex, gemini-cli, openai, anthropic, openrouter). Cursor/copilot/opencode/kimi/grok/zai/deepseek/ollama/lm-studio/perplexity lack Rust crates (JS normalizers only in collectors/providers.mjs).
4. Fake/demo generators: `apps/desktop-ui/src/lib/mock.ts` (via fetchDemoSnapshot only, SAMPLE-labeled); `prototype/server.mjs hash32` deterministic percents behind `GET /api/snapshot?demo=1` with sample_data/demo_mode flags. No production fallback-to-mock found; WP2 must prove by failure injection.
5. Persistence and schema: `crates/usage-halo-storage/migrations/0001_init.sql` (provider_accounts, usage_events, quota_snapshots, connector_health, daily_rollups) + 0002_canonical + 0003_session_states; `crates/usage-halo-storage/src/lib.rs` (db ~/.usagehalo/usagehalo.db, INSERT OR IGNORE, stable_fingerprint). Missing V2 tables: provider_sources, provider_snapshots, metric_observations, activity_events.
6. Polling/timer logic: UI `App.svelte setInterval 15s` + manual retry; `collectors/scheduler.mjs` nominal/max-healthy-age; `collectors/daemon.mjs due()` event-vs-poll; Rust `usage-halo-scheduler schedule()` mirror; `ConnectorHost::is_due/collect_due` stagger + class backoff (auth 1h, rate-limit 5m, network 60s, parse 600s/1h). Per-connection poll_interval in types.ts/AddProviderWizard.
7. API routes: prototype `/api/health|snapshot|sandbox?demo=1|activity-buckets|quotas|projection|activity|detected|registry|ingest/claude|ingest/event|snapshot/legacy-import|rollups|forecast|alerts|widget|codex/live|daemon|snapshot-cache`, `POST /v1/metrics|/api/ingest/gemini`; Tauri `invoke(snapshot|projection_overview|projection_activity|connector_health|runtime_info|...)`. V2 routes (`/api/providers*`, diagnostics, history, SSE events) do not exist yet.
8. Dashboard/filter data flow: api.ts liveProviderToView -> App.svelte merged (catalog x snapshot x connections, resolveStage) -> connected/detectedOnly/available/healthy/stale -> totals (connected+enabled+healthy only) -> cards/rail/popover; RangePicker+range.ts filter models/buckets -> TrendChart/Heatmap/DetailModal; same ProjectionService feeds Tauri + /api/projection|activity.
9. Auth/credential handling: alias-only (`usage-halo-secrets`, os-keychain feature, .env.example dev fallback), pollers return live:false keyless, local.mjs no secret reads, privacy_audit.py + test_privacy.py. No raw tokens in DB per schema.
10. Provider registration/toggle state: provider_accounts(enabled, secret_alias, connector_id); projection registry read-only; collector MANAGED list; Tauri snapshot/connector_health; UI USE_LIVE + AddProviderWizard; store.mjs file dedupe. No per-source enable/priority/backoff persistence yet; no settings/doctor surface.
11. Test and E2E runners: pytest (test_registry|snapshot|showcase|truth_phase0-6|parity|workflows|next|daemon|live_smoke|migration|realistic_data), scripts/validate.py, privacy_audit.py, live_smoke.mjs, run_checks.sh/.ps1; cargo test/clippy/fmt (toolchain absent here); npm run check|build|tauri (npm.cmd/node fallback).
12. Exact commands to start the app: `node prototype/server.mjs` (http://127.0.0.1:4897); `cd demo; python -m http.server 8080`; `cd apps/desktop-ui; npm install; npm run dev` (:1420 proxy :4897); `node collectors/local.mjs --json`; `node collectors/daemon.mjs --once|--loop`; ingest curl with fixtures/claude-statusline.json (see subagent report for full lines).
13. Exact files to change in each work package: WP1 core/contracts.ts + ProviderCard truth states; WP2 api.ts/server.mjs Real-vs-Demo boundary + failure test; WP3 storage migrations 0004 + Storage + projection history queries + range.ts; WP4 scheduler/collector cadence + backoff persist + App.svelte intervals; WP5-8 per-provider connector crates + collectors normalizers + fixtures/* + registry entries; WP9/11 secondary + runtime crates; WP12 settings API+Svelte; WP13 doctor API+Svelte; WP14 capability cards; WP15 failure matrix tests; WP16 delete mock/hash32 production reachability.
