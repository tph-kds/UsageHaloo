# Provider registry

`providers.json` is the UI/provider metadata registry. It intentionally uses neutral monograms in this scaffold.

Production should replace placeholder marks only after a brand/legal review. The accent value belongs to UsageHalo UI decoration, not to an assertion about official provider brand colors.
